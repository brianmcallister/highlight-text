/* eslint-disable */

type Text = string;
type Subject = Text | HTMLElement;
type Tag = string;
type Word = string;
type Words = Word[];
type Bound = number;
type Boundary = [Bound, Bound];
type Boundaries = Boundary[];
